name: 加速版构建 6.6.89 欧加真OKI内核

env:
  TZ: UTC
  ANDROID_VERSION: 'android15'
  KERNEL_VERSION: '6.6'
  KERNEL_NAME: 'android15-8-gf4dc45704e54-abogki446052083-4k'
  KERNEL_FULL: '6.6.89-android15-8-gf4dc45704e54-abogki446052083-4k'
  CCACHE_KEY: ccache-neov4-6.6.89

on:
  workflow_dispatch:
    inputs:
      ksu_type:
        description: 'KernelSU分支(ReSukiSU/SukiSU Ultra/KernelSU Next/KSU(原版)/无内置KSU,默认ReSukiSU)'
        required: true
        type: choice
        default: 'resukisu'
        options:
          - 'resukisu'
          - 'sukisu'
          - 'ksunext'
          - 'ksu'
          - 'none'
      susfs_enable:
        description: '是否开启susfs'
        required: true
        type: boolean
        default: 'true'
      kpm_enable:
        description: '是否开启kpm'
        required: true
        type: choice
        default: 'false'
        options:
          - 'false'
          - 'builtin'
          - 'kpn'
      lz4_enable:
        description: 'lz4 1.10.0+zstd 1.5.7'
        required: true
        type: boolean
        default: 'true'
      lz4kd_enable:
        description: 'LZ4KD'
        required: true
        type: boolean
        default: 'false'
      bbr_enable:
        description: 'BBR算法'
        required: true
        type: choice
        default: 'false'
        options:
          - 'false'
          - 'true'
          - 'default'
      better_net:
        description: '网络增强'
        required: true
        type: boolean
        default: 'false'
      adios_enable:
        description: 'ADIOS IO调度'
        required: true
        type: boolean
        default: 'true'
      rekernel_enable:
        description: 'Re-Kernel'
        required: true
        type: boolean
        default: 'false'
      baseband_guard:
        description: '基带保护'
        required: true
        type: boolean
        default: 'true'
      ccache_update:
        description: '更新ccache'
        required: true
        type: boolean
        default: 'false'
      ccache_debug:
        description: 'ccache日志'
        required: true
        type: boolean
        default: 'false'
      kernel_suffix:
        description: '内核后缀'
        required: false
        type: string
        default: ''

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      ksuver: ${{ steps.ksu_version.outputs.ksuver }}
      ak3name: ${{ steps.create_zip.outputs.ak3name }}
    permissions:
      actions: write
      contents: read
    steps:
      - name: 初始化环境
        run: |
          rm -rf kernel_workspace
          mkdir kernel_workspace
          cd kernel_workspace
          sudo apt-mark hold firefox libc-bin
          sudo apt purge man-db -y
          sudo rm -rf /var/lib/man-db/auto-update
          sudo apt update
          sudo apt-get install -y binutils python-is-python3 libssl-dev libelf-dev ccache
          
          aria2c -s16 -x16 https://github.com/cctv18/android_kernel_common_oneplus_sm8750/archive/refs/heads/oneplus/sm8750_v_16.0.0_oneplus_13_6.6.89.zip -o common.zip
          unzip -q common.zip
          mv android_kernel_common_oneplus_sm8750-oneplus-sm8750_v_16.0.0_oneplus_13_6.6.89 common
          rm common.zip
          
          mkdir -p clang18
          aria2c -s16 -x16 https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang18-r510928/clang-r510928.zip -o clang.zip
          unzip -q clang.zip -d clang18
          rm clang.zip
          
          aria2c -s16 -x16 https://github.com/cctv18/oneplus_sm8650_toolchain/releases/download/LLVM-Clang18-r510928/build-tools.zip -o build-tools.zip
          unzip -q build-tools.zip
          rm build-tools.zip
          
          rm common/android/abi_gki_protected_exports_* 2>/dev/null
          for f in common/scripts/setlocalversion; do
            sed -i 's/ -dirty//g' "$f"
            sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' "$f"
          done

      # ==============================================
      # 🔧 关键修复：强制写入完整内核版本号
      # ==============================================
      - name: 强制设置完整内核版本
        run: |
          cd kernel_workspace/common
          echo "EXTRAVERSION = -android15-8-gf4dc45704e54-abogki446052083-4k" >> Makefile
          echo "CONFIG_LOCALVERSION=\"\"" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_LOCALVERSION_AUTO=n" >> arch/arm64/configs/gki_defconfig
          sed -i 's/^CONFIG_LOCALVERSION.*/CONFIG_LOCALVERSION=""/' arch/arm64/configs/gki_defconfig

      - name: 配置ccache
        run: |
          echo "CCACHE_DIR=$HOME/.ccache_6.6.89" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=3G" >> $GITHUB_ENV
          df -h

      - name: 载入ccache缓存
        uses: actions/cache@v4
        id: ccache-restore
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ env.CCACHE_KEY }}-${{ runner.os }}-main
          restore-keys: |
            ${{ env.CCACHE_KEY }}-${{ runner.os }}-

      - name: 清理旧缓存
        if: inputs.ccache_update
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh cache delete ${{ env.CCACHE_KEY }}-${{ runner.os }}-main -R ${{ github.repository }} 2>/dev/null || true

      - name: 初始化ccache
        run: |
          export CCACHE_COMPILERCHECK="none"
          export CCACHE_BASEDIR="${{ github.workspace }}"
          export CCACHE_NOHASHDIR="true"
          export CCACHE_HARDLINK="true"
          export CCACHE_DIR="${{ env.CCACHE_DIR }}"
          export CCACHE_MAXSIZE="3G"
          mkdir -p "$CCACHE_DIR"
          ccache -M 3G
          ccache -o compression=true
          ccache -s

      - name: 添加KernelSU
        id: ksu_version
        run: |
          cd kernel_workspace
          if [[ ${{ github.event.inputs.ksu_type }} == "sukisu" ]]; then
            curl -LSs https://raw.githubusercontent.com/ShirkNeko/SukiSU-Ultra/main/kernel/setup.sh | bash -s builtin
            cd KernelSU
            GIT_COMMIT_HASH=$(git rev-parse --short=8 HEAD)
            KSU_API_VERSION=$(curl -s https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/builtin/kernel/Kbuild | grep -m1 "KSU_VERSION_API :=" | awk -F= '{print $2}' | tr -d '[:space:]' || echo 3.1.7)
            KSUVER=$(expr $(git rev-list --count main) + 37185 2>/dev/null || echo 114514)
          elif [[ ${{ github.event.inputs.ksu_type }} == "resukisu" ]]; then
            curl -LSs https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh | bash -s main
            echo 'CONFIG_KSU_FULL_NAME_FORMAT="%TAG_NAME%-%COMMIT_SHA%@cctv18"' >> common/arch/arm64/configs/gki_defconfig
            cd KernelSU
            KSUVER=$(expr $(git rev-list --count main) + 30700 2>/dev/null || echo 114514)
          elif [[ ${{ github.event.inputs.ksu_type }} == "ksunext" ]]; then
            curl -LSs https://raw.githubusercontent.com/pershoot/KernelSU-Next/dev-susfs/kernel/setup.sh | bash -s dev-susfs
            KSUVER=114514
          elif [[ ${{ github.event.inputs.ksu_type }} == "ksu" ]]; then
            curl -LSs https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh | bash -s main
            KSUVER=114514
          else
            KSUVER=0
          fi
          echo "ksuver=$KSUVER" >> $GITHUB_OUTPUT

      - name: 应用SUSFS
        if: inputs.susfs_enable
        run: |
          cd kernel_workspace
          if [[ ${{ github.event.inputs.ksu_type }} != "none" ]]; then
            git clone --depth=1 https://github.com/cctv18/susfs4oki.git susfs4ksu -b oki-${{ env.ANDROID_VERSION }}-${{ env.KERNEL_VERSION }}
            cp susfs4ksu/kernel_patches/50_add_susfs_in_gki-${{ env.ANDROID_VERSION }}-${{ env.KERNEL_VERSION }}.patch common/
            cp -r susfs4ksu/kernel_patches/fs/. common/fs/
            cp -r susfs4ksu/kernel_patches/include/linux/. common/include/linux/
            cd common
            patch -p1 < 50_add_susfs_in_gki-${{ env.ANDROID_VERSION }}-${{ env.KERNEL_VERSION }}.patch 2>/dev/null || true
          fi

      - name: 应用lz4/zstd
        if: inputs.lz4_enable
        run: |
          cd kernel_workspace
          git clone --depth=1 https://github.com/$GITHUB_REPOSITORY.git -b $GITHUB_REF_NAME src
          cp src/zram_patch/001-lz4.patch src/zram_patch/lz4armv8.S src/zram_patch/002-zstd.patch common/
          cd common
          git apply 001-lz4.patch 2>/dev/null || true
          patch -p1 < 002-zstd.patch 2>/dev/null || true

      - name: 配置内核选项
        run: |
          cd kernel_workspace
          echo "CONFIG_KSU=y" >> common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y" >> common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_HEADERS_INSTALL=n" >> common/arch/arm64/configs/gki_defconfig
          sed -i 's/check_defconfig//' common/build.config.gki

          if [[ ${{ github.event.inputs.kpm_enable }} == 'builtin' && ( "${{ github.event.inputs.ksu_type }}" == "sukisu" || "${{ github.event.inputs.ksu_type }}" == "resukisu" ) ]]; then
            echo "CONFIG_KPM=y" >> common/arch/arm64/configs/gki_defconfig
          fi

      - name: 构建内核（固定时间）
        run: |
          WORKDIR="$(pwd)"
          export PATH="$WORKDIR/kernel_workspace/clang18/bin:$PATH"
          export PATH="$WORKDIR/kernel_workspace/build-tools/bin:$PATH"
          cd kernel_workspace/common

          export TZ=UTC
          export KBUILD_BUILD_TIMESTAMP="Fri Sep 19 06:13:40 UTC 2025"
          export SOURCE_DATE_EPOCH=1758262420

          make -j$(nproc) LLVM=1 ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CC="ccache clang" \
            LD="ld.lld" \
            O=out gki_defconfig

          make -j$(nproc) LLVM=1 ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CC="ccache clang" \
            LD="ld.lld" \
            O=out Image

      # ===================== KPM镜像修补 =====================
      - name: 应用KPM并修补内核
        run: |
          if [[ ${{ github.event.inputs.kpm_enable }} == 'builtin' && ( "${{ github.event.inputs.ksu_type }}" == "sukisu" || "${{ github.event.inputs.ksu_type }}" == "resukisu" ) ]]; then
            echo "正在应用KPM并修补内核..."
            cd kernel_workspace/common/out/arch/arm64/boot
            curl -LO https://github.com/SukiSU-Ultra/SukiSU_KernelPatch_patch/releases/latest/download/patch_linux
            chmod +x patch_linux
            ./patch_linux
            rm -f Image
            mv oImage Image
          fi
      # ======================================================

      - name: 保存缓存
        if: inputs.ccache_update || steps.ccache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ${{ env.CCACHE_KEY }}-${{ runner.os }}-main

      - name: 打包AnyKernel3
        id: create_zip
        run: |
          cd kernel_workspace
          git clone https://github.com/cctv18/AnyKernel3 --depth=1
          rm -rf AnyKernel3/.git
          cp common/out/arch/arm64/boot/Image AnyKernel3/
          cd AnyKernel3

          if [[ ${{ github.event.inputs.kpm_enable }} == 'kpn' ]]; then
            wget https://github.com/cctv18/KPatch-Next/releases/latest/download/kpn.zip
          fi

          KSU_TYPENAME=${{ github.event.inputs.ksu_type }}
          AK3_NAME=AnyKernel3_${KSU_TYPENAME}_${{ steps.ksu_version.outputs.ksuver }}_${{ env.KERNEL_FULL }}.zip
          zip -r ../$AK3_NAME .
          echo "ak3name=$AK3_NAME" >> $GITHUB_OUTPUT

      - name: 上传内核
        uses: actions/upload-artifact@v7
        with:
          path: kernel_workspace/*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
      - name: 创建发布
        uses: ncipollo/release-action@v1
        with:
          tag: build-${{ github.run_id }}
          name: ${{ env.KERNEL_FULL }} 固定时间版内核
          body: |
            KPM支持: ${{ github.event.inputs.kpm_enable }}
          draft: false
          artifacts: "*.zip"
          token: ${{ secrets.GITHUB_TOKEN }}
